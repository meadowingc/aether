# Example environment variables for Aether Phoenix

# Phoenix / Cookie signing secret (generate with: mix phx.gen.secret)
SECRET_KEY_BASE=REPLACE_WITH_SECRET

# Basic auth credentials for Kaffy admin
ADMIN_USER=admin
ADMIN_PASS=change_me

# Optional production settings
# DATABASE_PATH=/var/lib/aether_phx/aether_phx.db
# PHX_HOST=example.com
# PORT=4000
# POOL_SIZE=5
# PHX_SERVER=true
